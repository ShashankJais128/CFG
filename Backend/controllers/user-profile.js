// Handling the update-user-profile page
exports.getUpdateUserProfilePage = (req, res, next) => {
    // url = req.url;
    // console.log("Requested url = ", req.url);
    // res.status(404).render("updateUserProfile", { pageTitle: "Update User Profile" });
    // console.log("User Profile Page is working");
    res.send("Update Profile page is working");
  };
